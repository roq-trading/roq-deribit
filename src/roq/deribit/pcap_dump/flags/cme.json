{
  "name": "roq/cme/import/flags/CME",
  "type": "flags",
  "prefix": "cme_",
  "values": [
    {
      "name": "config_file",
      "type": "std/string",
      "validator": "roq/flags/validators/Path",
      "required": true,
      "description": "CME's multicast channel XML config file."
    },
    {
      "name": "secdef_file",
      "type": "std/string",
      "validator": "roq/flags/validators/Path",
      "description": "CME's instrument definitions file (required when the .pcap file does not contain instrument definitions)."
    }
  ]
}
